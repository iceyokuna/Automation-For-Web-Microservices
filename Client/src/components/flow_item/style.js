import styled from 'styled-components'
import { Box } from 'grommet';

export const Snackbar = styled.div`
  position: absolute;
  bottom: 10px;
  right: 10px;
` 

export const FlowType = styled(Box)`
  position: absolute;
  top: 10px;
  right: -18px;
  border-radius: 100%;
  padding: 10px;
  `